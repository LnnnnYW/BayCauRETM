% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_pre.R
\name{preprocess_data}
\alias{preprocess_data}
\title{Preprocess Long‑Format Data for Causal Recurrent‑Event Modeling}
\usage{
preprocess_data(df, K, lag_col = NULL)
}
\arguments{
\item{df}{A \emph{long‑format} \code{data.frame} that \strong{already contains} the analysis
columns \code{pat_id}, \code{k_idx}, \code{Y_obs}, \code{T_obs}, \code{A}, plus any additional
covariates.}

\item{K}{Integer. Total number of discrete intervals per subject.}

\item{lag_col}{Optional character string. If specified, the function will
#'        create a lagged variable \code{lag_col} based on the previous value of
#'        \code{Y_obs} (i.e., \code{lag_col = "Y_prev"} will create \code{Y_prev}).}
}
\value{
A list with components:
\itemize{
\item \strong{processed_df} – The input data with filled‑in intervals, lagged
variables \code{Y_prev}/\code{T_prev}, a binary \code{lagY_bin}, and post‑death rows
removed.
\item \strong{n_pat} – Integer, number of unique subjects.
}
}
\description{
Fills missing intervals, creates lags, and runs basic checks.
}
\details{
Prepares a long‑format dataset (already using standardised column names) for
discrete‑time Bayesian causal modelling of recurrent and terminal events.
\itemize{
\item Fills every subject’s record to the full grid \code{k_idx = 1:K};
\item Carries treatment status \code{A} forward in time (\code{tidyr::fill(direction = "down")});
\item Removes all rows \strong{after} the first \code{T_obs == 1} to ensure monotone
terminal indicators;
\item Creates \code{T_prev}, \code{Y_prev}, and a helper indicator \code{lagY_bin = I(Y_prev > 0)};
\item Runs basic validity checks (duplicate \verb{(pat_id,k_idx)}, coding, ranges, missing values).
The data \strong{must already} follow the naming convention \verb{*_obs} for observed
quantities. The function does \emph{not} rename columns – it only pads, truncates,
lags, and validates.
}
}
\examples{
df_long <- data.frame(
  pat_id = rep(1:5, each = 3),
  k_idx  = rep(1:3, times = 5),
  Y_obs  = sample(0:2, 15, TRUE),
  T_obs  = rbinom(15, 1, 0.1),
  A      = rbinom(15, 1, 0.5),
  age    = rnorm(15, 60, 10)
)
\dontrun{
out <- preprocess_data(df_long, K = 3, x_cols = "age")
str(out)
}

}
\keyword{#}
\keyword{Preprocess}
\keyword{causal_recur}
\keyword{data}
\keyword{for}
\keyword{if}
\keyword{index}
\keyword{internal}
\keyword{it}
\keyword{keep}
\keyword{like}
\keyword{off}
\keyword{the}
\keyword{you}
